using UnityEngine;

public class Enemy_Manager : MonoBehaviour
{
    public void Initialize()
    {
        //reference map_manager and build patrol zones within the boundaries of the map.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
